import type { VendorName } from "./types";

/** Display label shown in the caption and rendered image. */
export const VENDOR_LABEL: Record<VendorName, string> = {
  indogold: "IndoGold",
  antam: "Antam",
  ubs: "UBS Gold (via IndoGold)",
};
